[project]
name = "bq"
version = "0.1.0"
description = "Budget-aware command queue"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
bq = "bq:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
